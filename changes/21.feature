Several API additions and improvements that came out of writing the
documentation: e.g. added support for the optional ``pixel_shape`` property
of a WCS, and iteration over sub-transforms of compound transforms (such
as ``compose`` and ``concatenate``). Support for ``frame_attributes`` on FK4
and FK5 celestial frames.
